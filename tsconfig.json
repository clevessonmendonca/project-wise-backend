{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2020",                          /* Define a versão do JavaScript para a qual o código será compilado. */
    "module": "commonjs",                         /* Especifica o sistema de módulo. */
    "rootDir": "./src",                           /* Define o diretório raiz do código-fonte como './src'. */
    "typeRoots": [
      "./node_modules/@types",
      "./src/types"
    ],                                            /* Define onde os arquivos de definição de tipo estão localizados. */
    "esModuleInterop": true,                      /* Facilita a interoperabilidade entre módulos CommonJS e ESModules. */
    "forceConsistentCasingInFileNames": true,     /* Garante a consistência de casos em imports de arquivos. */
    
    /* Emit */
    "emitDeclarationOnly": false,                 /* Não emite apenas arquivos de declaração. */
    "sourceMap": true,                            /* Cria arquivos de mapa de origem para facilitar a depuração. */
    "outDir": "./dist",                           /* Define o diretório de saída dos arquivos compilados como './dist'. */

    /* Type Checking */
    "strict": true,                               /* Habilita todas as verificações estritas de tipos. */

    /* Completeness */
    "skipLibCheck": true                          /* Ignora a verificação de arquivos .d.ts. */
  },
  "include": [
    "src/**/*.ts",                                /* Inclui todos os arquivos TypeScript dentro do diretório 'src'. */
    "./src/prisma/**/*.ts"                              /* Inclui todos os arquivos TypeScript dentro do diretório 'prisma'. */
  ],
  "exclude": [
    "node_modules",                               /* Exclui a pasta 'node_modules' da compilação. */
    "dist"                                        /* Exclui a pasta 'dist' da compilação. */
  ]
}
